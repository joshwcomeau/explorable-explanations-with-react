import styled from 'styled-components';
import { COLORS } from '../../constants';

export default styled.span`
  border-bottom: 12px solid ${COLORS.orange[500]};
  margin-bottom: -20px;
  line-height: 1.5;
  font-weight: bold;
`;
